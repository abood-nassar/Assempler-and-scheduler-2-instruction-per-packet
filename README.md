# 🧩 MIPS Assembler & Instruction Scheduler

This project implements a **MIPS assembler and instruction scheduler** using **Python**.
The assembler reads MIPS assembly code from a **text input file**, translates it into **binary and hexadecimal machine code**, and writes the results to an output file.
A scheduler then processes this output and organizes instructions into **dual-issue packets** while avoiding data hazards.

---

## 📥 Input & 📤 Output Files

### 📄 Input File
- **input.txt**
- Contains MIPS assembly instructions
- Supports labels, comments, and pseudo-instructions

Example:

ADDI $1, $0, -5
BEQ $2, $3, L1

📄 Output Files
1️⃣ output.txt

Generated by the assembler.
Each line shows:

Original instruction

Binary representation

Hexadecimal representation

Example:

ADDI $1, $0, -5 --> [ binary: 001000_00000_00001_1111111111111011, hex: 2001FFFB ]

2️⃣ scheduled_instructions.txt

Generated by the scheduler.
Instructions are grouped into dual-issue packets:

Packet 1:
  Slot1: ADDI $1, $0, -5 --> [ ... ]
  Slot2: ADDI $2, $0, 5 --> [ ... ]

🗂️ Project Structure
├── Assembler.py              # MIPS assembler
├── scheduler.py              # Dual-issue instruction scheduler
├── input.txt                 # Input assembly code
├── output.txt                # Assembler output (binary + hex)
├── scheduled_instructions.txt# Scheduled instruction packets

⚙️ How It Works
🔹 Assembler

Reads instructions from input.txt

Handles:

R-type, I-type, and J-type instructions

Labels and branches

Pseudo-instructions (e.g., BLT, BGE, MOVE, LI)

Produces binary and hexadecimal machine code

🔹 Scheduler

Reads instructions from output.txt

Detects data hazards

Classifies special instructions (branches, jumps, memory ops)

Schedules instructions into Slot 1 and Slot 2

Inserts NOP when needed to preserve correctness

▶️ How to Run

Make sure Python is installed:

python --version


Run the assembler:

python Assembler.py


Run the scheduler:

python scheduler.py


Check the results in:

output.txt

scheduled_instructions.txt
